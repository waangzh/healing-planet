package com.healingplanet.ai.query;

/** Independent state evidence needs that may be combined in one query. */
public enum StateNeed {
    CURRENT,
    HISTORY,
    FRESHNESS,
    DECISION_SUPPORT
}
